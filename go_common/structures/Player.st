structure Player{
  String first_name,
  String second_name,
  Int matches,
  Int full_matches,
  Int substituted,
  Int entered,
  Int minutes,
  Int goals,
  Int penalty,
  Int passes,
  Int yellow_cards,
  Int red_cards

}
